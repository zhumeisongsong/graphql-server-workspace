import { usersPresentationDto } from './users-presentation-dto';

describe('usersPresentationDto', () => {
  it('should work', () => {
    expect(usersPresentationDto()).toEqual('users-presentation-dto');
  });
});
