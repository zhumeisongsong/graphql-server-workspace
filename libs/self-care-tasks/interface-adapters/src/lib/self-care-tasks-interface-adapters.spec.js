import { selfCareTasksInterfaceAdapters } from './self-care-tasks-interface-adapters';
describe('selfCareTasksInterfaceAdapters', () => {
  it('should work', () => {
    expect(selfCareTasksInterfaceAdapters()).toEqual(
      'self-care-tasks-interface-adapters',
    );
  });
});
